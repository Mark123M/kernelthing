
### CUDA documentation — the `{{MCP_SERVER}}` MCP server

{{MCP_DESCRIPTION}}

It answers questions about the API and the hardware, not about your kernel — it has
never seen your code and holds none of your measurements.

If no `{{MCP_TOOL}}` tool appears in your tool list, the server is unauthenticated in
this sandbox. Skip it and move on; do not retry it.
