# Nsight Compute captures downloaded by `popcorn submit --profile-brev`. Candidates
# commit with `git add -A`, and these are large.
profile/
*.ncu-rep
*.zip

# Archived run artifacts. Most of the weight is members/*/opencode.ndjson (13M of
# 14M for a 25-candidate run) -- agent transcripts, not results. To keep the
# experiment record in git and drop only the transcripts, swap this for:
#   runs/**/opencode.ndjson
runs/
